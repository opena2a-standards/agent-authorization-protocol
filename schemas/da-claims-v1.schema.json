{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://specs.opena2a.org/schemas/aap/da-claims-v1.schema.json",
  "title": "AAP Delegation Assertion claim set (AAP-SPEC section 5.3)",
  "description": "Claims of the DA: the CGT claim set plus the RFC 8693 delegation members. `sub` is the delegatee, `act.sub` the delegating agent (nesting `act` expresses a chain, innermost first, per RFC 8693 section 4.1), `max_depth` bounds further delegation, `delegator_atx` embeds the delegator's ATX hash for the audit trail (AAP-SPEC section 5.2). The delegatee scope MUST be a subset of the delegator's scope; subsetting is enforced by the minting broker and re-checked by verifiers — the schema pins presence and form, not the subset relation. Self-contained: duplicates the CGT members rather than $ref-ing them so verification never requires reference resolution.",
  "type": "object",
  "required": ["iss", "sub", "aud", "scope", "trust_class", "issuer_chain", "trust_level", "act", "max_depth", "delegator_atx", "iat", "exp", "jti"],
  "properties": {
    "iss": {
      "type": "string",
      "minLength": 1
    },
    "sub": {
      "type": "string",
      "pattern": "^did:"
    },
    "aud": {
      "type": "string",
      "minLength": 1
    },
    "scope": {
      "type": "string",
      "minLength": 1
    },
    "trust_class": {
      "type": "string",
      "pattern": "^[a-z0-9_-]+:[a-z0-9_-]+$"
    },
    "issuer_chain": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "pattern": "^did:"
      }
    },
    "trust_level": {
      "type": "integer",
      "minimum": 0,
      "maximum": 4
    },
    "act": {
      "$ref": "#/$defs/actor"
    },
    "max_depth": {
      "type": "integer",
      "minimum": 0,
      "description": "Remaining delegation depth; 0 is a terminal delegation (AAP-SPEC 0.5 section 5.3). MUST NOT exceed the delegator's matching peer_agent subDelegationDepth (section 5.4; a verifier check)."
    },
    "delegator_atx": {
      "type": "string",
      "pattern": "^sha256:[0-9a-f]{64}$"
    },
    "iat": {
      "type": "integer",
      "minimum": 0
    },
    "exp": {
      "type": "integer",
      "minimum": 0
    },
    "jti": {
      "type": "string",
      "pattern": "^[0-9a-f]{32}$"
    },
    "aap_ver": {
      "type": "integer",
      "minimum": 1
    },
    "authorization_details": {
      "$ref": "#/$defs/authorizationDetails"
    },
    "aap_crit": {
      "$ref": "#/$defs/aapCrit"
    },
    "cnf": {
      "$ref": "#/$defs/cnf"
    },
    "fga_constraints": {
      "type": "string",
      "deprecated": true,
      "description": "DEPRECATED in AAP-SPEC 0.5; replacedBy `authorization_details`. Optional-to-ignore. No implementation minted it (FGC program audit, 2026-09-08)."
    }
  },
  "$defs": {
    "authorizationDetails": {
      "type": "array",
      "minItems": 1,
      "description": "RFC 9396 authorization_details typed by the AAP-SPEC section 4.4.1 registry; the wire value of `type` is the registry URI `https://specs.opena2a.org/aap/types/<name>`. In a DA every entry MUST be narrower than or equal to a delegator entry of the same type and no entry may lack a parent (section 5.4); the schema pins presence and form, the attenuation relation is a verifier check. Duplicated from cgt-claims-v1 so the DA schema stays self-contained.",
      "items": {
        "type": "object",
        "required": ["type"],
        "properties": {
          "type": { "type": "string", "pattern": "^https://specs\\.opena2a\\.org/aap/types/[a-z_]+$" },
          "requiresApproval": { "type": "boolean" },
          "locations": { "type": "array", "items": { "type": "string", "minLength": 1 } },
          "actions": { "type": "array", "items": { "type": "string", "minLength": 1 } },
          "datatypes": { "type": "array", "items": { "type": "string", "minLength": 1 } },
          "identifier": { "type": "string", "minLength": 1 },
          "privileges": { "type": "array", "items": { "type": "string", "minLength": 1 } },
          "egressCeiling": { "type": "array", "uniqueItems": true, "items": { "type": "string", "minLength": 1 } },
          "labelCeiling": { "type": "array", "uniqueItems": true, "items": { "type": "string", "minLength": 1 } }
        }
      }
    },
    "aapCrit": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": { "type": "string", "minLength": 1 }
    },
    "cnf": {
      "type": "object",
      "oneOf": [
        { "required": ["jwk"] },
        { "required": ["jkt"] }
      ],
      "properties": {
        "jwk": { "type": "object", "required": ["kty"] },
        "jkt": { "type": "string", "pattern": "^[A-Za-z0-9_-]{43}$" }
      }
    },
    "actor": {
      "type": "object",
      "required": ["sub"],
      "properties": {
        "sub": {
          "type": "string",
          "pattern": "^did:"
        },
        "act": {
          "$ref": "#/$defs/actor"
        }
      }
    }
  }
}
